Adds callouts on the integrations overview and OAuth setup pages noting that Metorial only provides default (managed) OAuth credentials for a subset of popular providers (Slack, GitHub, Google, etc.). Other providers may require BYO via Enterprise BYO.